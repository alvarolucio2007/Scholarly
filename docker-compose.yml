services:
  db:
    image: postgres:17-alpine
    container_name: pg-db
    environment:
      POSTGRES_DB: scholarly
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: root
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d scholarly"]
      interval: 5s
      timeout: 5s
      retries: 5
volumes:
  db-data:
